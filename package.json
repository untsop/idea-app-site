{
  "scripts": {
    "start": "set NODE_ENV=development && ./node_modules/.bin/gulp",
    "dev": "set NODE_ENV=development && ./node_modules/.bin/gulp clean && ./node_modules/.bin/gulp dev",
    "build": "NODE_ENV=production gulp build"
  },
  "engines": {
    "node": "12.x"
  },
  "dependencies": {
    "browser-sync": "^2.26.13",
    "del": "^3.0.0",
    "gulp": "^4.0.2",
    "gulp-postcss": "^9.0.0",
    "gulp-preprocess": "^2.0.0",
    "gulp-pug": "^4.0.1",
    "postcss": "^8.1.6",
    "postcss-nested": "^5.0.1",
    "tailwindcss": "^2.0.0-alpha.2"
  }
}
